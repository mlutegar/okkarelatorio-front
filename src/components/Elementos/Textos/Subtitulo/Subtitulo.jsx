import { SubtituloStyle } from "./Style";

const Subtitulo = ({children}) => {
    return (
        <SubtituloStyle>
            {children}
        </SubtituloStyle>
    );
}

export default Subtitulo;